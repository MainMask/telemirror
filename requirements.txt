telethon==1.41.2
opencv-python-headless
pillow
# simple-lama-inpainting: see install.sh (broken pillow<10 pin, install with --no-deps)
# torch: transitive dep of simple-lama-inpainting, install manually if needed:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
# ffmpeg: apt install ffmpeg
cryptg==0.5.2
psycopg[binary,pool]==3.2.12
python-decouple==3.8
PyYAML==6.0.3
aiohttp==3.13.1
uvloop==0.22.1; platform_system != "Windows"