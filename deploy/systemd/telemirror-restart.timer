[Unit]
Description=telemirror — daily clean restart of the live mirror

[Timer]
OnCalendar=*-*-* 04:00:00
# Don't catch up a missed run: restarting the mirror right after boot is pointless.
Persistent=false
RandomizedDelaySec=300

[Install]
WantedBy=timers.target
