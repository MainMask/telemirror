# Ставится bootstrap.sh в /etc/systemd/journald.conf.d/. Ограничивает журнал,
# чтобы логи 24/7-сервисов не забили диск. bootstrap создаёт /var/log/journal
# (persistent) — тогда работает SystemMaxUse; до того журнал в tmpfs — RuntimeMaxUse.
[Journal]
SystemMaxUse=500M
RuntimeMaxUse=200M
