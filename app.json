{
    "name": "TeleMirror",
    "description": "Telegram message forwarder (client API)",
    "repository": "https://github.com/MainMask/telemirror",
    "keywords": [
        "python",
        "telegram",
        "forwarder"
    ],
    "addons": [
        "heroku-postgresql"
    ],
    "env": {
        "API_ID": {
            "description": "Telegram APP ID"
        },
        "API_HASH": {
            "description": "Telegram APP hash"
        },
        "SESSION_STRING": {
            "description": "Telegram session string value"
        },
        "CHAT_MAPPING": {
            "description": "Mapping between source and target channels (with their IDs (-100...)). Example: [-100source1,-100source2:-100target];[-100source3:-100target2];"
        },
        "YAML_CONFIG_ENV": {
            "description": "(Optional) Single-line YAML mirror config; overrides CHAT_MAPPING and the REMOVE_URLS*/DISABLE_* vars",
            "required": false
        },
        "REMOVE_URLS": {
            "description": "Remove URLs from incoming messages (true or false). Defaults to false",
            "value": "false",
            "required": false
        },
        "REMOVE_URLS_LIST": {
            "description": "Comma-separated list of URLs to remove (reddit.com,youtube.com)",
            "required": false
        },
        "REMOVE_URLS_WL": {
            "description": "Comma-separated list of URLs to exclude from removal (google.com,twitter.com). Will be applied after the REMOVE_URLS_LIST",
            "required": false
        },
        "DISABLE_DELETE": {
            "description": "Disable mirror message deleting (true or false). Defaults to false",
            "value": "false",
            "required": false
        },
        "DISABLE_EDIT": {
            "description": "Disable mirror message editing (true or false). Defaults to false",
            "value": "false",
            "required": false
        },
        "USE_MEMORY_DB": {
            "description": "Use an in-memory database instead of Postgres DB (true or false). Defaults to false",
            "value": "false",
            "required": false
        },
        "PAST_MODE": {
            "description": "(Optional) Replay history on startup: last_n=N | full_history | since_date=YYYY-MM-DDTHH:MM:SS",
            "required": false
        },
        "BROADCAST_CHANNEL": {
            "description": "(Optional) Channel ID whose messages are synced to all configured targets on startup and live",
            "required": false
        },
        "BROADCAST_TARGETS": {
            "description": "(Optional) Comma-separated whitelist of broadcast targets (-100id or -100id#topic_id)",
            "required": false
        },
        "BROADCAST_SEND_DELAY": {
            "description": "(Optional) Delay in seconds between sends during broadcast sync. Defaults to 0.5",
            "required": false
        },
        "TECH_CHANNEL": {
            "description": "(Optional) Channel ID that receives WARNING+ log alerts and DM notifications",
            "required": false
        },
        "SEND_DELAY": {
            "description": "(Optional) Delay in seconds between sends for live mirroring. Defaults to 0.5",
            "required": false
        },
        "API_SYSTEM_VERSION": {
            "description": "(Optional) Telegram client system version override (anti-ban)",
            "required": false
        },
        "API_DEVICE_MODEL": {
            "description": "(Optional) Telegram client device model override (anti-ban)",
            "required": false
        },
        "API_APP_VERSION": {
            "description": "(Optional) Telegram client app version override (anti-ban)",
            "required": false
        },
        "LOG_LEVEL": {
            "description": "Logging level (debug, info, warning, error or critical). Defaults to info",
            "value": "info",
            "required": false
        }
    }
}
